class Element {
      String keyword; //Holds keyword used to identify element
      Article head; // Stores reference to record object
      
      Element(String k, Article r){ // used to start
         keyword = k;
         head = r;
      }
   }
     